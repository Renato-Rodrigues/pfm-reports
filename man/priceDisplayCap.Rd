% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reportHelpers.R
\name{priceDisplayCap}
\alias{priceDisplayCap}
\title{Shared price-chart display cap anchored near a target (ADR 0032)}
\usage{
priceDisplayCap(x, target = 1000)
}
\arguments{
\item{x}{Numeric price vector (pooled across the charts that should share a range).}

\item{target}{Anchor in USD/tCO2. Default 1000.}
}
\value{
A single numeric cap.
}
\description{
Picks whichever of p99 / p99.5 / p99.9 of the positive values is closest to \code{target}
\emph{without exceeding it}; falls back to \code{target} if even p99 is above it. This is a
\emph{disclosed display} cap for readability (NOT the model's extrapolation clamp); use one common
value across all price charts so they share a y-range and a few extrapolation outliers don't squash
the visible range.
}
