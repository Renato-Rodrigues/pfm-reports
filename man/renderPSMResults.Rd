% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/renderPSM.R
\name{renderPSMResults}
\alias{renderPSMResults}
\title{Render the PSM Results report (consumes a PSM Run-Group)}
\usage{
renderPSMResults(
  group = getPfmConfig("group", "psm-exhaustive"),
  resultsDir = .defResults(),
  reportName = group,
  indexMax = 10,
  outputDir = .defOutput(),
  verbose = TRUE
)
}
\arguments{
\item{group}{PSM Run-Group name (e.g. \code{"psm-exhaustive"}).}

\item{resultsDir}{Results Root containing the Run-Group.}

\item{reportName}{Output filename suffix. Default = \code{group}.}

\item{indexMax}{Index ceiling used at fit time (CAPMF: 10).}

\item{outputDir}{Directory for the rendered HTML.}

\item{verbose}{Logical.}
}
\value{
Path to the rendered HTML (invisibly).
}
\description{
One comprehensive report for a Policy Stringency Model Run-Group: sweep +
maximin selection, the bounded-index Projection-Sanity trace, the deployed
spec's coefficients, the estimator-agreement exhibit (satP engine /
fractional-logit headline / beta / levels), and the scenario fan-out
projections with the Implementability Factor. Every section degrades
gracefully when its artifact is missing, so the report can be rendered at any
pipeline stage.
}
